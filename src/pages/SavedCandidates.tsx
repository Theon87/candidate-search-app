const SavedCandidates = () => {
  // create a table to display the potential candidates in a table format
  

  // create a function to load the potential candidates from local storage

  // create a function to remove a potential candidate from the list of potential candidates

  return (
    <>
      <h1>Potential Candidates</h1>
      {/*WHEN the potential candidates page loads
        THEN the user should see a list of previously saved potential
        candidates with their name, username, location, avatar, email, html_url, and company*/}



      {/*WHEN the page reloads
        THEN the list of potential candidates should persist and be available for viewing*/}
      
      
      {/*WHEN there are no potential candidates
        THEN an appropriate message should be displayed indicating no candidates have been accepted*/}
      

    </>
  );
};

export default SavedCandidates;